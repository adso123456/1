# 第 3 级 P0 manual_review 样本修正收口报告

## 基础信息

- 当前工作目录：E:\3\posgresql\1
- 远端仓库：origin https://github.com/adso123456/1.git
- 当前 commit（修正前）：1a3973b1fe78d012a259c59bc90e0a0c1a8b1176
- 修正日期：2026-07-10

## 本阶段操作范围

| 项目 | 值 |
|------|-----|
| 是否启动真实主服务 | 否 |
| 是否连接数据库 | 否 |
| 是否执行真实 SQL | 否 |
| 是否调用 DeepSeek | 否 |
| 是否训练 Vanna | 否 |
| 是否调用 vn.train() | 否 |
| 是否写入正式 ChromaDB | 否 |
| 是否只修正 4 条 manual_review 样本 | 是 |

## 修正明细

### L3_P0_SQL_004 — pH 年均值排序

| 字段 | 修正前 | 修正后 |
|------|--------|--------|
| question | 查询年度水质pH均值最优站点排名 | 查询年度pH年均值最高的站点列表 |
| business_intent | 站点排名…做排序排名 | pH年均值极值排序…不表达水质最优判断 |
| risk_notes | …以 pH 均值降序表示最优站点 | …不表示pH越高水质越优 |

**修正原因**：pH不是越高越好（中性~7为佳），原question将"最高"等同于"最优"是业务语义错误。修正后为纯数据排序查询。

**SQL**：未修改。

### L3_P0_SQL_007 — 近两年月趋势

| 字段 | 修正前 | 修正后 |
|------|--------|--------|
| question | 查看某站点pH和溶解氧在最近12个月的变化趋势 | 查看某站点近两年pH和溶解氧月变化趋势 |
| business_intent | 最近12个月趋势 | 近两年月度趋势 |
| risk_notes | …固定年份为避免模型学到硬编码年份 | …monitor_year>=2025是示例年份条件（当前年份2026，>=2025覆盖近两年） |

**修正原因**：原question"最近12个月"与SQL `monitor_year>=2025 LIMIT 24` 时间语义不匹配。修正后question与SQL一致。

**SQL**：未修改。

### L3_P0_SQL_013 — 溶解氧固定阈值

| 字段 | 修正前 | 修正后 |
|------|--------|--------|
| question | 某站点小时记录中溶解氧偏低的时段 | 查询某站点小时记录中溶解氧低于5.0mg/L的时段 |
| business_intent | 阈值过滤查询…处理偏低/低于类比较语义 | 固定阈值过滤查询…处理数值比较类语义 |
| risk_notes | …5.0仅为示例阈值，不暗示实际溶解氧标准 | …5.0是示例阈值，不代表所有业务场景的溶解氧偏低标准 |

**修正原因**：原question中的"偏低"是主观判断且5.0阈值未经业务确认。修正后在question中直接写明阈值数值，去掉主观判断词。

**SQL**：未修改。

### L3_P0_SQL_017 — 水质等级筛选

| 字段 | 修正前 | 修正后 |
|------|--------|--------|
| question | 查询月度水质达标站点列表 | 查询月度水质为I至III类的站点列表 |
| business_intent | 达标筛选…处理达标/合格类语义 | 水质等级筛选…不表达达标判断 |
| risk_notes | …表示达标等级（I~III类为优良），不意味着实际水质标准 | …是等级筛选条件，不等同于所有场景的达标口径 |

**修正原因**：原question中"达标"一词与SQL `IN('I','II','III')`的映射未经业务确认（达标标准取决于水体功能区划）。修正后去掉"达标"判断词。

**SQL**：未修改。

## 修正原则

- **全部只改 question / business_intent / risk_notes，不改 SQL**
- 修正方向：去掉主观判断词（最优、最近、偏低、达标），改为客观描述
- 保留 SQL 结构训练价值不变

## 修正后静态检查

```text
重新运行 check_sql_examples_level3_p0.py
18 PASS, 0 WARNING, 0 FAIL
SQL Guard passed=True: 18/18
SQL Guard severity=ok: 18/18
```

## 修正后审查结论

```text
approved: 18
requires_manual_review: 0
excluded: 0
```

## 结论

**4 条原 manual_review 样本全部修正完成，question 与 SQL 语义已对齐。全部 18 条可进入下一阶段（P0 训练写入）。**
